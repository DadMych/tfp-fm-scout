/** Default transfer budget before the user runs the Assistant (doc 17 §8.5). */
export const DEFAULT_BUDGET = 50e6;

/** Default tactic preset when the user has not run the Assistant yet (doc 17 §9.1). */
export const DEFAULT_FORMATION_ID = "4-2-3-1";

/** Default registered-squad size after a window (doc 20). */
export const DEFAULT_SQUAD_CAP = 25;
